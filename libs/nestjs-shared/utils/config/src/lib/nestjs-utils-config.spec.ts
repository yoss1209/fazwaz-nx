import { nestjsUtilsConfig } from './nestjs-utils-config';

describe('nestjsUtilsConfig', () => {
  it('should work', () => {
    expect(nestjsUtilsConfig()).toEqual('nestjs-utils-config');
  });
});
