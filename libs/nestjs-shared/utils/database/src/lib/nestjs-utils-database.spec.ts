import { nestjsUtilsDatabase } from './nestjs-utils-database';

describe('nestjsUtilsDatabase', () => {
  it('should work', () => {
    expect(nestjsUtilsDatabase()).toEqual('nestjs-utils-database');
  });
});
