import { nestjsUtilsLogger } from './nestjs-utils-logger';

describe('nestjsUtilsLogger', () => {
  it('should work', () => {
    expect(nestjsUtilsLogger()).toEqual('nestjs-utils-logger');
  });
});
